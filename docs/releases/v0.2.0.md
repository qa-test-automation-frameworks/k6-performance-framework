# v0.2.0

Evidence-focused release of the k6 Performance Framework.

## Highlights

- Failure-sensitive performance regression checks with measured baseline validation.
- Open and closed workload models with graceful shutdown behavior.
- Endpoint-scoped SLOs, recorded business metrics, and richer summary artifacts.
- Weighted external scenario data and validated environment workload controls.
- Retained observability data, run annotations, dashboard filters, and business KPI panels.
- Exact dependency versions, strict TypeScript boundaries, secret scanning, and pre-commit gates.

## Release Requirements

- Tag the verified commit as `v0.2.0`.
- Capture and review three controlled full-load summaries before requiring regression protection.
